<div class="container home-page" style="max-width: 1400px;">

  <%# ===== Store Hero (Apple Store style) ===== %>
  <section class="sb-store-hero sb-full-bleed"
           style="background-image: url('<%= asset_path("snapbid_banner_combined_v2.png") %>');">
    <div class="container" style="max-width: 1400px;">
      <div class="d-flex flex-column flex-md-row align-items-md-start justify-content-between gap-4 sb-hero-content">
        <h1 class="sb-store-title mb-0">Cửa Hàng</h1>

        <div class="text-md-end">
          <div class="sb-store-sub mb-0">
            Săn deal phút chót,<br>chốt giá thông minh.
          </div>
          <a class="sb-store-link" href="#browse">
            Khám phá sản phẩm <i class="bi bi-arrow-right"></i>
          </a>
        </div>
      </div>
    </div>
  </section>

  <%# ===== Category icons row ===== %>
  <div class="sb-store-cats-wrap sb-full-bleed mb-4">
    <div class="container" style="max-width: 1400px;">
      <%
        # Icons verified in Bootstrap Icons 1.11.3 — order matches seed categories:
        # Áo, Quần, Giày, Dép, Phụ kiện, Túi xách, Adidas, Dior, Chanel, Nike, Gucci
        cat_icons = %w[
          bi-tag-fill
          bi-tags-fill
          bi-person-walking
          bi-sun
          bi-gem
          bi-handbag
          bi-lightning-fill
          bi-star-fill
          bi-heart-fill
          bi-play-fill
          bi-crown
        ]
      %>

      <div class="sb-store-cats">
        <% (@categories || []).each_with_index do |cat, idx| %>
          <a class="sb-cat" href="<%= root_path(category_id: cat.id, q: @q, condition: @condition, sort: @sort) %>">
            <span class="sb-cat-icon"><i class="bi <%= cat_icons[idx % cat_icons.length] %>"></i></span>
            <span class="sb-cat-label"><%= cat.name %></span>
          </a>
        <% end %>

        <a class="sb-cat" href="<%= root_path %>">
          <span class="sb-cat-icon"><i class="bi bi-grid-3x3-gap"></i></span>
          <span class="sb-cat-label">Tất cả</span>
        </a>
      </div>
    </div>
  </div>

  <div id="browse"></div>

  <%# ===== Section header + Search ===== %>
  <div class="d-flex flex-wrap align-items-end justify-content-between gap-2 mb-3">
    <div class="me-auto">
      <h2 class="fw-semibold fs-4 mb-1">Sản phẩm đang đấu giá</h2>
      <div class="text-muted small">
        Ưu tiên xem “Sắp kết thúc” để không bỏ lỡ.
      </div>
    </div>

    
  </div>

  <%# ===== Summary + Mobile filter button ===== %>
  <div class="d-flex align-items-center justify-content-between gap-2 mb-3">
    <div class="text-muted small">
      <% total = (@listings.respond_to?(:total_count) ? @listings.total_count : @listings.size) %>
      <span class="fw-semibold"><%= total %></span> sản phẩm
      <% if @q.present? %>
        <span class="ms-1">· Kết quả cho: <span class="fw-semibold">"<%= @q %>"</span></span>
      <% end %>
    </div>

    <div class="d-md-none">
      <button class="btn btn-outline-secondary btn-sm"
              type="button"
              data-bs-toggle="offcanvas"
              data-bs-target="#homeFilters"
              aria-controls="homeFilters">
        <i class="bi bi-sliders"></i>
        Bộ lọc
      </button>
    </div>
  </div>

  <%# ===== Desktop Filter Bar (sticky dưới navbar) ===== %>
  <div class="card shadow-sm mb-4 bg-light border-0 d-none d-md-block sb-sticky-under-nav">
    <div class="card-body py-3 home-filter-card">
      <form method="get" action="<%= root_path %>" class="row g-2 align-items-end">
        <input type="hidden" name="q" value="<%= @q %>">

        <div class="col-md-3">
          <label class="form-label small text-muted mb-1"><%= t("home.filters.category") %></label>
          <select name="category_id" class="form-select form-select-sm" onchange="this.form.requestSubmit()">
            <option value=""><%= t("home.filters.all") %> - <%= t("home.filters.category") %></option>
            <% (@categories || []).each do |cat| %>
              <option value="<%= cat.id %>" <%= "selected" if @category_id.to_s == cat.id.to_s %>>
                <%= cat.name %>
              </option>
            <% end %>
          </select>
        </div>

        <div class="col-md-3">
          <label class="form-label small text-muted mb-1"><%= t("home.filters.condition") %></label>
          <select name="condition" class="form-select form-select-sm" onchange="this.form.requestSubmit()">
            <option value=""><%= t("home.filters.all") %> - <%= t("home.filters.condition") %></option>
            <% (@conditions || []).each do |c| %>
              <option value="<%= c %>" <%= "selected" if @condition == c %>><%= c %></option>
            <% end %>
          </select>
        </div>

        <div class="col-md-3">
          <label class="form-label small text-muted mb-1"><%= t("home.filters.sort") %></label>
          <select name="sort" class="form-select form-select-sm" onchange="this.form.requestSubmit()">
            <option value="newest" <%= "selected" if @sort == "newest" %>><%= t("home.filters.newest") %></option>
            <option value="ending_soon" <%= "selected" if @sort == "ending_soon" %>>Sắp kết thúc</option>
            <option value="price_asc" <%= "selected" if @sort == "price_asc" %>>Giá hiện tại ↑</option>
            <option value="price_desc" <%= "selected" if @sort == "price_desc" %>>Giá hiện tại ↓</option>
          </select>
        </div>

        <div class="col-md-3 d-flex gap-2">
          <a href="<%= root_path %>" class="btn btn-outline-secondary btn-sm flex-grow-1">
            <i class="bi bi-arrow-counterclockwise"></i> Reset
          </a>
        </div>
      </form>
    </div>
  </div>

  <%# ===== Mobile Offcanvas Filters ===== %>
  <div class="offcanvas offcanvas-end" tabindex="-1" id="homeFilters" aria-labelledby="homeFiltersLabel">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title" id="homeFiltersLabel"><i class="bi bi-sliders"></i> Bộ lọc</h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Đóng"></button>
    </div>
    
  </div>

  <%# ===== Grid ===== %>
  <% if @listings.blank? %>
    <div class="card border-0 shadow-sm">
      <div class="card-body p-4 text-center">
        <div class="display-6 mb-2">😕</div>
        <div class="fw-semibold mb-1">Chưa có sản phẩm phù hợp</div>
        <div class="text-muted mb-3">Thử đổi bộ lọc hoặc tìm kiếm từ khoá khác nhé.</div>
        <a class="btn btn-outline-primary" href="<%= root_path %>">
          <i class="bi bi-arrow-counterclockwise"></i> Reset bộ lọc
        </a>
      </div>
    </div>
  <% else %>
    <div class="row g-3">
      <% @listings.each do |listing| %>
        <%= render "listings/grid_card", listing: listing %>
      <% end %>
    </div>

    <div class="d-flex justify-content-center mt-5 mb-4">
      <%= paginate @listings %>
    </div>
  <% end %>

</div>
