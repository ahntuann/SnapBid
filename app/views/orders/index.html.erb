<h1>Đơn hàng của tôi</h1>

<ul>
  <% @orders.each do |o| %>
    <li>
      <%= link_to "Order ##{o.id}", order_path(o) %> -
      <%= o.kind %> -
      <%= o.status %> -
      <%= o.price %>
    </li>
  <% end %>
</ul>
