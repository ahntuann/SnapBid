<h1><%= t("pages.seller.listings.edit_title") %></h1>

<%= render "form",
  listing: @listing,
  url: seller_listing_path(@listing),
  submit_text: t("buttons.update") %>

<p><%= link_to t("buttons.back"), seller_listing_path(@listing) %></p>
